Architecture
============

The system follows a three-layer architecture:

1. **Web Layer (Flask)**: Web UI and control endpoints.
2. **ETL Layer (Scrape, Clean, Load)**: Data ingestion pipeline.
3. **DB Layer (SQLAlchemy & Native SQLite)**: ORM models and query functions.
