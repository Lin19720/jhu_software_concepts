[pytest]
markers =
    web: Tests for web endpoints and routes.
    buttons: Tests for UI buttons and interactions.
    analysis: Tests for analysis calculation and formatting.
    db: Tests for database connections and queries.
    integration: End-to-end integration flow tests.
